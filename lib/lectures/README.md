# Лекции коллегии (единственный источник текста)

Редактируй **только** `1.json` … `4.json`. UI **не читает** Supabase `lectures`.

После правок подними `LECTURE_CONTENT_REVISION` в `lib/college-lectures.ts`.

Сборка проверяет маркеры (Ишанго, аль-Хорезми, …) — укороченная версия не пройдёт.
